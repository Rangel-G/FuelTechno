# ==========================================
# FuelTechno - Dependências
# ==========================================

# Comunicação Serial (OBD-II)
pyserial>=3.5

# Bluetooth Low Energy (LED RGB)
bleak>=3.0.2
bleak-retry-connector>=4.6.1

# Comunicação WebSocket entre bridge.py e interface
websockets>=16.1

# Servidor HTTP (caso utilizado)
Flask>=3.1.0
Flask-Cors>=6.0.0

# Variáveis de ambiente (.env)
python-dotenv>=1.1.0

# Janela nativa do app único (main.py) — abre a interface sem navegador/CMD
# e é a base para gerar o .exe
pywebview>=6.2.1
# Driver D2XX da FTDI (mesmo mecanismo do FORScan; conecta pelo serial do chip,
# sem depender de porta COM/VCP)
ftd2xx>=1.3.3

# Cloud Firestore (sincronismo de configurações via Admin SDK)
firebase-admin>=7.5.0